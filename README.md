# PasswordGen
A simple Java console app that checks password strength based on length, uppercase, lowercase, digits, and special characters. It also checks if the password contains personal info and rates it as weak, medium or strong. If needed, it can generate a secure random password automatically.
